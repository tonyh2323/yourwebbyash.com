const greeting = 'Hello, world!';
function sendGreeting(name) {
  let greeting;
console.log(name);
  if(name) {
    greeting = `Hello, ${name}!`;
    console.log(greeting);
    return greeting;
    
  } else {
    
    return greeting;
    
  };
  
};

sendGreeting( );
