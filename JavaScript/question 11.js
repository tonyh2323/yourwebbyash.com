const button = document.createElement('button');
button. onclick  = () => {
    const header = document.querySelector("h1");
    header.appendChild = "Hello, World!";
    document.body.getElement(header);
}